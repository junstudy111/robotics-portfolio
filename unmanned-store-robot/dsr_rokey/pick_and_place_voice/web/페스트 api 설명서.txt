1️⃣ 노트북 (서버 역할) - [여기만 세팅하면 끝!]
노트북은 웹 서버이자 로봇 제어기 역할을 합니다. 모든 파일은 노트북에 있어야 합니다.

Step 1. 폴더 및 파일 준비
홈 폴더(Home) 안에 아래 구조대로 파일들이 있는지 확인해 주세요.

Plaintext

~/robot_project/
├── fastapi_server.py       (👉 서버 실행 프로그램)
└── static/                 (👉 웹 화면 및 이미지 폴더)
    ├── dashboard_final.html
    ├── waiting.gif
    ├── listening.gif
    ├── not_heard.gif
    ├── refund_guide.png
    └── image_1c2cfc.png 등... (신발 이미지들)
Step 2. 필수 프로그램 설치 (터미널)
한 번만 설치하면 됩니다. (이미 하셨다면 패스)

Bash

# 1. 오디오 관련 시스템 패키지 설치 (우분투 필수)
sudo apt-get update
sudo apt-get install python3-pyaudio portaudio19-dev

# 2. 파이썬 라이브러리 설치
pip install fastapi uvicorn python-multipart websockets opencv-python qrcode[pil] SpeechRecognition pyaudio
Step 3. 실행 명령어 (매번 할 때)
서버를 켜는 명령어입니다.

Bash

# 1. 폴더로 이동
cd ~/robot_project

# 2. ROS2 환경 설정 (필요한 경우)
source /opt/ros/humble/setup.bash

# 3. 서버 실행!
python3 fastapi_server.py
👉 실행하면 터미널에 거대한 QR 코드가 뜹니다.

2️⃣ 태블릿 (리모컨 역할)
태블릿은 노트북에 있는 화면을 보기만 하는 역할입니다.

와이파이 확인: 태블릿이 노트북과 **같은 와이파이(공유기)**에 연결되었는지 확인하세요.

카메라 켜기: 기본 카메라 앱을 켭니다.

QR 코드 스캔: 노트북 화면(터미널)에 뜬 QR 코드를 찍습니다.

접속: 링크를 누르면 크롬 브라우저가 열리면서 신발 매장 화면이 뜹니다.
